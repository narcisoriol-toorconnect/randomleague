# 🗄️ Database Layer (Block C)

**Responsibility**: The Source of Truth.

This directory contains the schema definitions and migration scripts. It defines *what* data is stored, independent of the database engine (though currently targeting **PostgreSQL/Supabase**).