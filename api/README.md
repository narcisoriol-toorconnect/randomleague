# 🌐 API Layer (Block B)

**Responsibility**: The Gateway and Orchestrator.

This directory contains the backend logic that serves the Client. It handles authentication, request validation, and business logic enforcement (e.g., checking if the league is locked).